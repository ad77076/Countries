import React from 'react'
import Screen from './Screen'   

export const Home = ({navigation}) => <Screen navigation={navigation} name="Home"/>
export const Exit = ({navigation}) => <Screen navigation={navigation} name="Exit"/>
